---
# defaults file for ansible-role-symlink-repo

git_repo: "https://github.com/bluk/dotfiles.git"
git_repo_local_dest: "~/dotfiles"
git_repo_dir_src: "home"

symlink_local_dest: "~"
symlink_local_force_replace: false
